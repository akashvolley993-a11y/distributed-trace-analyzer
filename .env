API_BASE_URL=http://localhost:8000
PORT=8000
HOST=0.0.0.0
# Optional: Set your OpenAI or other LLM API key if desired.
# The LangGraph RCA agent runs with built-in heuristic reasoning if this is omitted.
OPENAI_API_KEY=
